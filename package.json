{
  "name": "story-slides-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Tap-to-advance story slides for structured content — Instagram-Stories-like UX for React, with embedded quizzes and a shareable deck protocol.",
  "license": "MIT",
  "workspaces": ["packages/*", "demo"],
  "scripts": {
    "typecheck": "npm run typecheck --workspace=story-slides-protocol && npm run typecheck --workspace=story-slides-ui"
  }
}
