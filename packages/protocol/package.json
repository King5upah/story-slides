{
  "name": "story-slides-protocol",
  "version": "0.3.0",
  "description": "Portable formats for story-slides decks: a versioned DeckManifest (serialize/validate/share via URL) plus ShareableDeck (file-based JSON, compatible with the Swift SDK). No React, no backend required.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["story-slides", "protocol", "deck", "serialization", "share"]
}
